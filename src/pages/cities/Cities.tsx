import React from "react";
import { PagesHeader } from "../../components";
import "./cities.css";

const Cities = () => {
  return (
    <div className="cities">
      <PagesHeader />
    </div>
  );
};

export default Cities;
